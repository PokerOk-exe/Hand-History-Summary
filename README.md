# Парсер Hand History ПокерОк для игроков

Инструмент для анализа раздач PokerOK на основе текстовых Hand History файлов. Скрипт автоматически парсит историю раздач, определяет участие игрока (Hero), вычисляет ключевые покерные метрики и выводит подробный отчёт в консоль. Дополнительно поддерживается экспорт статистики в CSV.

---

## 🚀 Возможности

Скрипт `handhistory.py` позволяет:

- 📁 Рекурсивно находить и обрабатывать файлы раздач (.txt, .log)  
- 👤 Анализировать игру конкретного игрока (Hero)  
- 🧮 Считать основной набор покерных статистик:  
  - **VPIP%** — добровольное вложение фишек в банк  
  - **PFR%** — префлоп агрессия  
  - **3-bet / Fold-to-3bet** *(MVP, может быть расширено)*  
  - **C-bet Flop% / Turn%**  
  - **WTSD%** — дошёл до шоудауна  
  - **W$SD%** — выиграл на шоудауне  
  - **Winrate (bb/100)**  
- 🪑 Формировать **позиционную статистику** (BTN, CO, MP, UTG, SB, BB)  
- 📊 Сохранять отчёты в CSV для последующего анализа  
- 📝 Работать полностью офлайн, без зависимостей от внешних библиотек  

---

## 📦 Установка

```bash
git clone https://github.com/PokerOk-exe/Hand-History-Summary.git
cd Hand-History-Summary
````

Скрипт не требует дополнительных библиотек — используется только стандартная библиотека Python.

---

## 🔧 Использование

Минимальный запуск:

```bash
python handhistory.py --path path/to/hands
```

### Полный список аргументов:

| Аргумент                 | Описание                                                    |
| ------------------------ | ----------------------------------------------------------- |
| `--path PATH`            | Путь к файлу или директории с Hand History *(обязательный)* |
| `--hero NAME`            | Имя героя (по умолчанию `Hero`)                             |
| `--output-prefix PREFIX` | Префикс для сохранения CSV-файлов                           |
| `--encoding ENC`         | Кодировка файлов (по умолчанию `utf-8`)                     |
| `--verbose`              | Подробный лог разбора HH                                    |

### Пример:

```bash
python handhistory.py \
  --path ./hands_pokerok \
  --hero Hero \
  --output-prefix result \
  --verbose
```

После выполнения команда сформирует отчёт в консоли, а также сохранит:

* `result_global_stats.csv`
* `result_positional_stats.csv`

---

## 📊 Пример консольного отчёта

```
=== PokerOK / GGNetwork Hand History Summary ===

Total hands: 12345
VPIP:        24.3 %
PFR:         20.1 %
Gap:          4.2 %

C-bet Flop:  68.4 %
C-bet Turn:  52.1 %
WTSD:        27.5 %
W$SD:        54.2 %

Total result: 1234.56 chips
Winrate:     5.3 bb/100

=== Positional Stats ===
Position   Hands   VPIP%   PFR%   bb/100
------------------------------------------
UTG        1234    18.2    15.0    3.2
MP         2345    20.1    16.8    4.5
CO         2500    26.7    22.5    6.0
BTN        2600    32.1    28.0    8.5
SB         1833    18.0    14.0   -1.0
BB         1833    15.5    10.3   -8.0
```

---

## 📁 Структура репозитория

```
Hand-History-Summary/
│
├── handhistory.py     # основной скрипт анализа
├── README.md          # документация
└── LICENSE            # MIT лицензия
```

---

## 🛠 Техническая информация

### Используется только стандартная библиотека:

* `argparse`
* `csv`
* `re`
* `pathlib`
* `logging`
* `collections`
* `statistics`
* `dataclasses`
* и др.

### Ограничения текущей версии (MVP)

* Парсер упрощён и рассчитан на типовой формат PokerOK HH
* Поддерживается NL Hold’em (кэш)
* Некоторые метрики (3-bet, fold-to-3bet) считаются базово и могут быть расширены

Планируется улучшение парсера под разные форматы HH PokerOK/GG.

---

## 🤝 Контрибьюция

Pull Request'ы приветствуются!
Если хотите улучшить парсер, добавить поддержку форматов HH или расширить статистику — открывайте Issue или отправляйте PR.

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Свободно используйте, модифицируйте и распространяйте.
